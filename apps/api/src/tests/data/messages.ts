import type { CreateMessageType } from "@repo/types/message";

export const message1: CreateMessageType = {
  message: "that's why we clash",
};
export const message2: CreateMessageType = {
  message: "for games like that",
};
